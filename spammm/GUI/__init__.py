"""__init__.py — PyQt5 GUI package for molecular editing and AFM simulation.

Contains the main application window (**SPAMMM_GUI**), reusable widget base
(**BaseGUI**), 3D molecular viewers (**MoleculeViewer**, **VispyUtils**, **GLGUI**),
lazy-loading extension system (**ExtensionManager**), and domain extensions
(**AFMExtension**, **FFExtension**, **QEqExtension**, **KekuleExtension**).
Also includes **MolecularBrowser** for database browsing and **ThumbnailCache**
for offscreen rendering. Pure-plotting utilities in **plotutils.py**.
"""
