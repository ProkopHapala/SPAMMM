# SPAMMM user guide

End-user documentation (how to run simulations **without** digging through `tests/` or opening the GUI).

| Doc | Topic |
|-----|--------|
| [SPM_CLI.md](SPM_CLI.md) | Headless AFM/STM CLI — **`run_spm.py`** (FDBM, Morse, Kriging, STM orbitals/current/panel) |

**Repo entry points:** root [`run_spm.py`](../run_spm.py) · task/gaps [`doc/Tasks/SPM_CLI_Headless.md`](../doc/Tasks/SPM_CLI_Headless.md) · agent ToDo [`doc/ToDo/ToDo.agents.md`](../doc/ToDo/ToDo.agents.md).

Developer / agent material (architecture, skills, campaign reports) stays under `doc/` and `doc/AGENTS/`.
