[pytest]
markers =
    gpu: marks tests that require a GPU (OpenCL)
    slow: marks tests that are slow (> 1s per test function)
    visual: marks tests that produce plots/XYZ for visual review (still have assertions)
