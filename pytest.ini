[pytest]
testpaths = tests
markers =
    gpu: marks tests that require a GPU (OpenCL)
    slow: marks tests that are slow (> 1s per test function)
    visual: marks tests that produce plots for human review (still have assertions)
    review: marks tests that emit L1 .out/.log artifacts when --review or --develop
addopts = -v --tb=short
